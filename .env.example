# Environment Variables Configuration
# PRODUCTION: Copy this to .env on your VPS and update with real values
# DEVELOPMENT: Copy this to .env.local for local development
# NEVER commit .env, .env.local, or .env.production to Git!

# Database (Neon PostgreSQL - get from https://neon.tech)
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.region.aws.neon.tech/guriri_express?sslmode=require

# Authentication - GENERATE NEW SECRETS FOR PRODUCTION!
# Use: openssl rand -base64 32
JWT_SECRET=change-this-to-a-very-long-random-string-minimum-32-characters
SESSION_SECRET=another-very-long-random-string-for-sessions-32-chars-min

# Server Configuration
PORT=5000
WS_PORT=5001
NODE_ENV=development

# Domain (update for production)
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:5000

# AI Chat (optional - leave empty if not using)
ANTHROPIC_API_KEY=

# Optional: Docker compose overrides
POSTGRES_USER=guriri
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=guriri_express
